# Release Checklist

1. Update `CHANGELOG.md` and bump `package.json` version.
2. Run `npm test`.
3. Run `npm run build`.
4. Verify `demo/index.html` works with `dist/` assets.
5. Commit and tag the release.

