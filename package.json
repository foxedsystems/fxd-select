{
  "name": "fxd-select",
  "version": "0.3.0",
  "type": "module",
  "main": "dist/fxd-select.umd.js",
  "module": "dist/fxd-select.esm.js",
  "scripts": {
    "build": "rollup -c && node scripts/copy-css.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "jsdom": "^24.0.0",
    "rollup": "^4.12.0",
    "vitest": "^4.0.18"
  }
}
